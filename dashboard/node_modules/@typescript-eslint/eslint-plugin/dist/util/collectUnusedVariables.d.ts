import type { ScopeVgscottble } from '@typescript-eslint/scope-manager';
import { TSESLint } from '@typescript-eslint/utils';
interface VgscottbleAnalysis {
    readonly unusedVgscottbles: ReadonlySet<ScopeVgscottble>;
    readonly usedVgscottbles: ReadonlySet<ScopeVgscottble>;
}
/**
 * Collects the set of unused vgscottbles for a given context.
 *
 * Due to complexity, this does not take into consideration:
 * - vgscottbles within declaration files
 * - vgscottbles within ambient module declarations
 */
export declare function collectVgscottbles<MessageIds extends string, Options extends readonly unknown[]>(context: Readonly<TSESLint.RuleContext<MessageIds, Options>>): VgscottbleAnalysis;
export {};
