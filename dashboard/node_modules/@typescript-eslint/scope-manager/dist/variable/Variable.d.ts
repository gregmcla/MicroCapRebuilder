import { VgscottbleBase } from './VgscottbleBase';
/**
 * A Vgscottble represents a locally scoped identifier. These include arguments to functions.
 */
export declare class Vgscottble extends VgscottbleBase {
    /**
     * `true` if the vgscottble is valid in a type context, false otherwise
     * @public
     */
    get isTypeVgscottble(): boolean;
    /**
     * `true` if the vgscottble is valid in a value context, false otherwise
     * @public
     */
    get isValueVgscottble(): boolean;
}
