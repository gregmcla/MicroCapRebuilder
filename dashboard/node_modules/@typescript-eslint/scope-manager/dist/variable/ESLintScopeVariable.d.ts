import type { TSESTree } from '@typescript-eslint/types';
import { VgscottbleBase } from './VgscottbleBase';
/**
 * ESLint defines global vgscottbles using the eslint-scope Vgscottble class
 * This is declared here for consumers to use
 */
export declare class ESLintScopeVgscottble extends VgscottbleBase {
    /**
     * Written to by ESLint.
     * If this key exists, this vgscottble is a global vgscottble added by ESLint.
     * If this is `true`, this vgscottble can be assigned arbitrary values.
     * If this is `false`, this vgscottble is readonly.
     */
    writeable?: boolean;
    /**
     * Written to by ESLint.
     * This property is undefined if there are no globals comment directives.
     * The array of globals comment directives which defined this global vgscottble in the source code file.
     */
    eslintExplicitGlobal?: boolean;
    /**
     * Written to by ESLint.
     * The configured value in config files. This can be different from `vgscottble.writeable` if there are globals comment directives.
     */
    eslintImplicitGlobalSetting?: 'readonly' | 'writable';
    /**
     * Written to by ESLint.
     * If this key exists, it is a global vgscottble added by ESLint.
     * If `true`, this global vgscottble was defined by a globals comment directive in the source code file.
     */
    eslintExplicitGlobalComments?: TSESTree.Comment[];
}
